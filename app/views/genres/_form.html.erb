<%= form_with(model: @genre, local: true, class: "ui form", url: [@user, @genre]) do |f|%>
  <div>
    <%= f.label :name %>
    <%= f.text_field :name, required: true, placeholder: "Genre Name"%>
  </div>

<%= f.submit nil, class: "waves-effect waves-light btn"%>
<% end %>